package columbia.plt.tt.typecheck;

import columbia.plt.tt.interpreter.symbolType;

public class CheckObjectType {
	static public checkObjectType (Object value, String type) {
		if (value instanceof String) return "string"
	}

}
